# Agents-Prisma

Multi-agent system for PRISMA systematic review pipeline using CrewAI.

## Quick Start

```bash
# Create virtual environment and install dependencies
uv venv && uv sync

# Activate and run
.venv\Scripts\activate
python src/main.py
```

## Project Structure

See `PROJECT.md` in `.planning/` for detailed architecture documentation.
